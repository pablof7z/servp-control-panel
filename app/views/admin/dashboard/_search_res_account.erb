<%= link_to h(result.name), :controller => '/accounts', :action => 'switch', :id => result.mask %><br />
<%= pluralize result.users.size, 'user' %><br />
<%= pluralize result.servers.size, 'server' %>
